{
  "name": "user-service",
  "version": "1.0.0",
  "description": "Play with passportjs",
  "main": "server.js",
  "scripts": {
    "start": "knex migrate:latest && node $NODE_DEBUG_OPTION server.js",
    "test": "NODE_ENV=test knex migrate:latest && NODE_ENV=test mocha --recursive --exit",
    "nyc": "NODE_ENV=test knex migrate:latest && NODE_ENV=test nyc mocha --recursive --exit",
    "nyc-report": "NODE_ENV=test knex migrate:latest && NODE_ENV=test nyc --reporter=html mocha --recursive --exit"
  },
  "author": "Ihor Banadiga",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LvivNodeJsClub/passport.git"
  },
  "keywords": [
    "passport",
    "node"
  ],
  "bugs": {
    "url": "https://github.com/LvivNodeJsClub/passport/issues"
  },
  "homepage": "https://github.com/LvivNodeJsClub/passport#readme",
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "knex": "^0.15.2",
    "passport": "^0.4.0",
    "pg": "^7.4.3",
    "require": "^2.4.20",
    "rootpath": "^0.1.2"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-as-promised": "^7.1.1",
    "http-status-codes": "^1.3.0",
    "mocha": "^5.2.0",
    "nyc": "^13.0.1",
    "sinon": "^6.1.5",
    "supertest": "^3.1.0"
  }
}
